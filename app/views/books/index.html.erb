<%= render 'layouts/error', book: @book %>
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render 'searche_form', user: current_user %>
      <%= render 'new', book: @book %>
    </div>
    <div class="col-md-8 offset-md-1">
      <%= render 'index', books: @books %>
    </div>
  </div>
</div>